numpy>=1.26,<3
scipy>=1.13,<2
sounddevice>=0.4.7,<1

# Phase 1+ integrations. Install after the Phase 0 check passes.
faster-whisper>=1.1,<2
sentence-transformers>=3,<4
scikit-learn>=1.5,<2
kokoro>=0.9,<1

# Phase 2+ integrations.
# pycaw/comtypes are what make levels and real mute/unmute possible; without
# them volume control falls back to nudging the multimedia keys. wmi/pywin32
# drive the monitor's root\WMI brightness interface the same way.
Pillow>=10,<13
pycaw>=20240210; sys_platform == "win32"
wmi>=1.5,<2; sys_platform == "win32"
pywin32>=311; sys_platform == "win32"
pytesseract>=0.3.10,<1
pygetwindow>=0.0.9,<1
pyautogui>=0.9.54,<1
playwright>=1.45,<2
openwakeword>=0.6,<1
PyQt5>=5.15,<6

# Optional Tier 2 LLM fallback (enable with NOVA_LLM_FALLBACK=1; needs the
# Ollama desktop app running, e.g. `ollama pull phi4-mini`).
ollama>=0.4,<1
