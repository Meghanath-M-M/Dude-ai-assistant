{
  "open_chrome": {
    "action": "open_app",
    "target": "chrome",
    "safe": true,
    "examples": ["open chrome", "launch the browser", "start google chrome", "open my browser"]
  },
  "open_vscode": {
    "action": "open_app",
    "target": "code",
    "safe": true,
    "examples": ["open vscode", "launch visual studio code", "start my code editor", "open vs code"]
  },
  "search_web": {
    "action": "browser_search",
    "safe": true,
    "examples": ["search the web for", "look up", "find this online", "google this"]
  },
  "system_volume": {
    "action": "system_control",
    "target": "volume",
    "safe": true,
    "examples": ["mute the volume", "turn up the sound", "lower my volume", "set volume to fifty percent"]
  },
  "read_screen": {
    "action": "screen_ocr",
    "safe": true,
    "examples": ["read the screen", "what does this say", "read this page to me", "what is on my screen"]
  },
  "open_project": {
    "action": "context_open",
    "safe": true,
    "examples": ["open my project", "launch my workspace", "open my ml folder", "open the project folder"]
  },
  "greeting": {
    "action": "greet",
    "safe": true,
    "examples": [
      "hello",
      "hi",
      "hey there",
      "good morning",
      "good afternoon",
      "bye",
      "goodbye",
      "see you later"
    ]
  },
  "time_check": {
    "action": "time_check",
    "safe": true,
    "examples": ["what time is it", "tell me the time", "what is the time", "current time"]
  },
  "repeat_last": {
    "action": "repeat_last",
    "safe": true,
    "examples": ["do that again", "repeat that", "do it again", "run that again"]
  },
  "set_project": {
    "action": "set_project",
    "safe": true,
    "examples": ["set project to", "remember this folder as", "set my project path"]
  },
  "set_browser": {
    "action": "set_browser",
    "safe": true,
    "examples": ["set browser to", "change my browser to", "use another browser"]
  },
  "set_preference": {
    "action": "set_preference",
    "safe": true,
    "examples": [
      "set wake threshold to",
      "enable dry run",
      "disable dry run",
      "go live",
      "switch to live mode",
      "set voice to",
      "use voice"
    ]
  },
  "close_window": {
    "action": "close_window",
    "safe": false,
    "examples": ["close this window", "close the window", "close the active window"]
  },
  "tidy_downloads": {
    "action": "tidy_downloads",
    "safe": false,
    "examples": ["clear my downloads", "tidy my downloads", "clean up my downloads"]
  },
  "lock_workstation": {
    "action": "lock_workstation",
    "safe": false,
    "examples": ["lock the computer", "lock my screen", "lock the workstation"]
  },
  "identity": {
    "action": "identity",
    "safe": true,
    "examples": [
      "what is your name",
      "what's your name",
      "who are you",
      "what should I call you"
    ]
  },
  "help": {
    "action": "help",
    "safe": true,
    "examples": [
      "help",
      "what can you do",
      "what can I ask you",
      "list your commands"
    ]
  }
}
